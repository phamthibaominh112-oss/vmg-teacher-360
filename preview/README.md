# Static UI preview

`teacher-ui-preview.html` is only for reviewing the teacher-facing visual/interaction direction without a backend.

Do **not** use this file as the production deployment. Production deployment is the Next.js app in `src/` connected to the Supabase backend defined in `supabase/`.
